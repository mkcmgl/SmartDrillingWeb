import { Vector } from '@int/geotoolkit/map/sources/Vector';
/**
 * Vector source that allows user to get features from remote resource in GeoJSON format
 */
export declare class GeoJSON extends Vector {
    /**
     * @param [options] options
     */
    constructor(options?: Vector.Options);
}
