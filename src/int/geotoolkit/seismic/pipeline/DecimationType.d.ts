/**
 * Enum of seismic pipeline sample interpolation types
 * @enum
 * @readonly
 */
export declare enum DecimationType {
    /**
     * Keep local extremum values
     */
    LocalExtremum = "LocalExtremum",
    /**
     * Average value
     */
    Average = "Average"
}
