/**
 * @module geotoolkit/seismic/pipeline/ClippingMode
 */
/**
 * Enum of seismic pipeline clipping modes
 * @enum
 * @readonly
 */
export declare enum ClippingMode {
    /**
     * Connected clipping mode
     */
    Connected = "Connected",
    /**
     * Disconnected clipping mode
     */
    Disconnected = "Disconnected"
}
